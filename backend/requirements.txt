fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic[email]>=2.8.0

pyjwt>=2.9.0
bcrypt>=4.2.0

openai>=1.40.0
python-dotenv>=1.0.1
requests>=2.32.0

PyMuPDF>=1.26.0

chromadb>=1.0.15

sentence-transformers>=5.0.0

# torch is a heavy dependency (~2GB with GPU wheels). This app only needs
# CPU inference for sentence-transformers, so deploy scripts (see
# render.yaml) install the CPU-only wheel separately before this file.
# torchvision/torchaudio are not used by this app and are intentionally
# omitted.
torch

langchain-text-splitters>=0.3.8

tiktoken>=0.9.0

tqdm>=4.67.0

typing-extensions>=4.12.0
